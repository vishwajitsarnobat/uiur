psycopg2-binary>=2.9
pgvector>=0.2.5
sentence-transformers>=2.7
spacy>=3.7
PyYAML>=6.0
scikit-learn>=1.4
umap-learn>=0.5
matplotlib>=3.8
numpy>=1.26
python-dotenv>=1.0

# Required separately (pip can't trigger this via requirements.txt):
#   python -m spacy download en_core_web_sm
#
# Postgres/pgvector itself runs via Docker (see docker-compose.yml), not pip --
# you don't need a local Postgres install.
